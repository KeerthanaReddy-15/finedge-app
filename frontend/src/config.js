/**
 * FinEdge Platform Configuration
 * Centralized API management for local and production environments.
 */

export const API_URL = 'http://localhost:5000';

export const config = {
    API_URL,
    // Add other global config values here
};

export default config;
